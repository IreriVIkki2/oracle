import React from "react";
import Head from "next/head";

const Home = () => (
    <Head>
        <title>oracle</title>
        <link rel="icon" href="/favicon.ico" />
    </Head>
);

export default Home;
